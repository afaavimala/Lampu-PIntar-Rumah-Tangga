# Cloudflare Worker production configuration reference
# Do NOT upload real secrets to repository.
#
# Non-secret vars (set in wrangler.toml [vars] or --var):
JWT_ACCESS_TTL_SEC=900
JWT_REFRESH_TTL_SEC=2592000
COOKIE_SECURE=true
COOKIE_SAME_SITE=None
COOKIE_DOMAIN=yourdomain.com
CORS_ORIGINS=https://your-dashboard.pages.dev,https://dashboard.yourdomain.com
MQTT_CLIENT_ID_PREFIX=smartlamp-worker-prod
AUTH_LOGIN_RATE_LIMIT_MAX=5
AUTH_LOGIN_RATE_LIMIT_WINDOW_SEC=60
COMMAND_SIGN_RATE_LIMIT_MAX=20
COMMAND_SIGN_RATE_LIMIT_WINDOW_SEC=60
SEED_ADMIN_EMAIL=admin@example.com
SEED_SAMPLE_DEVICE_ID=lampu-ruang-tamu

# Secrets (set using `wrangler secret put`):
# JWT_SECRET=...
# HMAC_GLOBAL_FALLBACK_SECRET=...
# MQTT_WS_URL=wss://your-cluster.s1.eu.hivemq.cloud:8884/mqtt
# MQTT_USERNAME=...
# MQTT_PASSWORD=...
# SEED_ADMIN_PASSWORD=...
