# Cloudflare Worker production configuration reference
# Do NOT upload real secrets to repository.
# Default di bawah diselaraskan dengan root `.env.example`.
# Untuk domain HTTPS publik, override minimal:
# - COOKIE_SECURE=true
# - COOKIE_SAME_SITE=None (jika frontend beda origin)
#
# Non-secret vars (set in wrangler.toml [vars] or --var):
JWT_ACCESS_TTL_SEC=900
JWT_REFRESH_TTL_SEC=2592000
COOKIE_SECURE=false
COOKIE_SAME_SITE=Strict
COOKIE_DOMAIN=
CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
MQTT_CLIENT_ID_PREFIX=smartlamp-worker
AUTH_LOGIN_RATE_LIMIT_MAX=8
AUTH_LOGIN_RATE_LIMIT_WINDOW_SEC=60
COMMAND_EXECUTE_RATE_LIMIT_MAX=30
COMMAND_EXECUTE_RATE_LIMIT_WINDOW_SEC=60
SEED_ADMIN_EMAIL=admin@example.com
SEED_SAMPLE_DEVICE_ID=lampu-ruang-tamu

# Secrets (set using `wrangler secret put`):
# JWT_SECRET=...
# MQTT_WS_URL=wss://your-cluster.s1.eu.hivemq.cloud:8884/mqtt
# MQTT_USERNAME=...
# MQTT_PASSWORD=...
# SEED_ADMIN_PASSWORD=...
